pre-commit:
  jobs:
    - name: ocaml
      glob: "*.{ml,mli,mly}"
      run: opam exec -- dune build && opam exec -- dune build @fmt && opam exec -- dune test
    - name: ts2pant
      glob: "tools/ts2pant/**/*.{ts,mts}"
      run: just ts2pant-precommit
